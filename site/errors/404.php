<?php

	/* Not sure if this is gonna work or not. But I figured we'd try and create our own custom 404 page.
		Might not work since the host is shit, but once moved it'll definitely work. */

	require_once('../global.include.php');
	require_once(HEADER);

	echo('<br>Temporary 404 page<br>');

	require_once(FOOTER);
